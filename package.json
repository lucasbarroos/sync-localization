{
  "requires": true,
  "scripts": {
    "start": "nodemon index.js",
    "production": "pm2 start index.js --name realtime-api"
  },
  "version": "0.0.1",
  "dependencies": {
    "express": "^4.16.4",
    "socket.io": "^2.2.0"
  }
}
